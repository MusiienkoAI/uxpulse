# RN SDK Snippet

Minimal drop-in example for React Native apps:

- screen tracking with explicit source hints
- API timing/error instrumentation
- batched event upload to `POST /v1/events/batch`

Use `uxpulse-sdk.ts` as a starting point for your app integration.
